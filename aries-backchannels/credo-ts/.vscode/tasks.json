{
    "version": "2.0.0",
    "tasks": [
        {
            "type": "shell",
            "label": "tsc: build - tsconfig.json",
            "command": "tsc",
            "args": ["-p", "server/tsconfig.json"],
            "problemMatcher": ["$tsc"]
        }
    ]
}